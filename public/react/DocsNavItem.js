import useRubicoVersion from './useRubicoVersion.js'

/**
 * @name DocsNavItem
 *
 * @synopsis
 * ```coffeescript [specscript]
 * DocsNavItem(props => {
 * }) -> ReactElement
 * ```
 */
const DocsNavItem = ReactElement(props => {
  // const [rubicoVersion] = useRubicoVersion()
  return Div()
})

export default DocsNavItem
